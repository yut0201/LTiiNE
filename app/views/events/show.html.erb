<h1>Event詳細</h1>
<h2>イベント名：<%= params[:event_name] %></h2>
<p>登録済みのプレゼンテーション一覧です。</p>
<table class="table table-hover">
  <thead class="thead-light">
    <tr>
      <th>プレゼンテーション名</th>
      <th>説明</th>
      <th>ユーザ名</th>
      <th>いいね！数</th>
      <th colspan="5"></th>
    </tr>
  </thead>

<% @presentations.each do |p| %>
  <tr>	
    <td><%= link_to p.presentation_name, presentation_path(p.id) %></td>
    <td><%= p.description %></td>
    <td><%= p.display_name %></td>
    <td><%= p.likes %></td>
    <td>
      <% if p.display_name == @user_name %>
        <%= link_to '削除', presentation_path(p.id), method: :delete, data: { confirm: 'このプレゼンテーションを削除しますか？' } %>
      <% end %>
    </td>
  </tr>
<% end %>
</table>
<p>プレゼンテーションの登録はこちら</p>
<%= link_to "プレゼン登録", new_event_presentation_path(@event.event_name), class: "btn btn-info" %>

